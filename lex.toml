[package]
name        = "lex-llm"
version     = "0.1.0"
description = "Pure-Lex LLM-agent runtime — provider abstraction, tool-call loop, and structured-output validation."

[dependencies]
"lex-schema" = "0.9.2"
"lex-trail"  = "0.1.0"
